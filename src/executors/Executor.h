#ifndef FAITHFUL_EXECUTOR_H
#define FAITHFUL_EXECUTOR_H

// does it have sense to make it static (template <int max_threads>)?
class Executor {
 private:
  // threads / fibers
};

#endif  // FAITHFUL_EXECUTOR_H
