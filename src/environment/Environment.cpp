#include "Environment.h"

#include "../entities/Objects_3d.h"
#include "../entities/Objects_2d.h"

namespace faithful {

Floor2D::Floor2D(DefaultType mode) {
  /*position_ = glm::vec3(0.0f, -0.9f, -10.0f);
  size_ = glm::vec3(2.0f, 0.2f, 1.0f);
  image_id_ = DefaultTextures::ids_[3];
  UpdatePosition();*/
}

Floor3D::Floor3D(DefaultType mode) {
//  position_ = glm::vec3(0.0f, -1.0f, 0.0f);
//  size_ = glm::vec3(1000.0f, 1.0f, 1000.0f);
//  image_id_ = DefaultTextures::ids_[3];
}

Wallpaper::Wallpaper(DefaultType mode) {
//  position_ = glm::vec3(0.0f, 0.0f, -10.0f);
//  size_ = glm::vec3(2.0f, 2.0f, 1.0f);
//  switch (mode) {
//    case DefaultType::Twilight:
//      image_id_ = DefaultTextures::ids_[5];
//      break;
//    case DefaultType::Map:
//      image_id_ = DefaultTextures::ids_[3];
//      break;
//  }
//  UpdatePosition();
}

} // namespace faithful
