
# TODO: add to README.md <---- IMPORTANT: all external libs are built with very specific
#   build options, so installation for each library is disabled
# todo: there may occur situation where some external lib was built but
#       without needed configurations (example: Assimp doesn't support needed format)

# libalsa - required libalsa-dev
#     (I'm not sure, but it wasn't compiled well for the first time)


# add explanation why some externals are in git submodule but some not